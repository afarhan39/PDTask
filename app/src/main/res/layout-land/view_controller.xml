<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/topNavBar"
    android:layout_width="70dp"
    android:layout_height="wrap_content">


    <ImageButton
        android:id="@+id/ibFlightTakeOff"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/ic_flight_takeoff"
        android:layout_marginBottom="24dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:layout_above="@id/ibFlightLand"
        android:layout_centerHorizontal="true"/>

    <ImageButton
        android:id="@+id/ibFlightLand"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/ic_flight_land"
        android:layout_marginBottom="24dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:layout_above="@id/ibJoystick"
        android:layout_centerHorizontal="true"/>

    <ImageButton
        android:id="@+id/ibJoystick"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/ic_joystick"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"/>


</RelativeLayout>
